[project]
name = "api-server"
version = "0.1.0"
description = "FastAPI Orchestrator for Vision RAG"
requires-python = ">=3.12"
dependencies = [
    "ai-core",
]

[tool.uv.sources]
ai-core = { workspace = true }